# --- Server Port ---
server.port=8080
# --- Offene API Endpunkte ---
app.security.permit-all-paths=/actuator/health

# --- OAuth2 Resource Server JWT ---
# BillingHub-WebApp hat einen EIGENEN Authentik-Provider/Application (Slug "billing-hub", siehe
# Office-OS/Authentik) statt den von ProjectManager-WebApp ("project-hub") mitzubenutzen.
spring.security.oauth2.resourceserver.jwt.issuer-uri=https://authentik.nexacode.de/application/o/billing-hub/
spring.security.oauth2.resourceserver.jwt.jwk-set-uri=http://authentik-svc:9000/application/o/billing-hub/jwks/

# Siehe Kommentar in billing-hub-dev.properties.
app.security.trusted-issuers=https://authentik.nexacode.de/application/o/internal-services/

# Service-Account fuer Service-zu-Service-Aufrufe (CustomerService, FileStorageService,
# ProjectManager): siehe app.security.internal-client.* in der zentralen
# application-prod.properties (gemeinsamer Service-Account fuer alle internen Aufrufer, daher
# nicht hier dupliziert).

# --- Datenbank ---
spring.datasource.url=jdbc:mariadb://billinghub_db:3306/billinghub
spring.datasource.username=billinghub
spring.datasource.password=Qn7bYWtxLPz4kM5vRj2FhDaN9uEsCz8x
spring.datasource.driver-class-name=org.mariadb.jdbc.Driver

# --- Billing-Events (RabbitMQ, aus ProjectManager) ---
# exchange muss exakt mit billing.rabbitmq.exchange bei ProjectManager uebereinstimmen.
billing.rabbitmq.exchange=${BILLING_RABBITMQ_EXCHANGE:billing.events.exchange}
billing.rabbitmq.queue=${BILLING_RABBITMQ_QUEUE:billing.events.billinghub.queue}
billing.rabbitmq.routing-key=${BILLING_RABBITMQ_ROUTING_KEY:#}
billing.rabbitmq.dlx=${BILLING_RABBITMQ_DLX:billing.events.billinghub.dlx}
billing.rabbitmq.dlq=${BILLING_RABBITMQ_DLQ:billing.events.billinghub.dlq}
